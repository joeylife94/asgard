export default function App() {
  return (
    <main className="app-shell">
      <section className="boot-card" aria-labelledby="asgard-title">
        <p className="eyebrow">Hybrid AI Operations Platform</p>
        <h1 id="asgard-title">Asgard</h1>
        <p className="status-copy">
          Frontend boot shell is running. Operational data will be connected in a later verified slice.
        </p>
      </section>
    </main>
  );
}
